# Cargo.toml
[package]
name = "libcrawler"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
scraper = "0.17"
tokio = { version = "1", features = ["rt"] }
libc = "0.2"
